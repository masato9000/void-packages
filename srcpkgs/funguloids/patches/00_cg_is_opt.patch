--- bin/plugins.cfg.in
+++ bin/plugins.cfg.in
@@ -8,6 +8,9 @@
 Plugin=RenderSystem_GL
 Plugin=Plugin_ParticleFX
 Plugin=Plugin_OctreeSceneManager
-Plugin=Plugin_CgProgramManager
+
+# The Cg plugin needs non-free software so we
+# disable it by default and enable it if present
+#Plugin=Plugin_CgProgramManager
 
 
--- src/ogreapp.cpp
+++ src/ogreapp.cpp
@@ -23,6 +23,7 @@
 //
 //***************************************************************************/
 
+#include <sys/stat.h>
 #include "ogreapp.h"
 #include "objectsystem.h"
 #include "effects.h"
@@ -73,6 +74,13 @@
 			OGRE_CONFIG_AND_LOG_PATH + "Ogre.log"
 			);
 
+	ConfigFile cfg;
+	cfg.load(String(OGRE_PLUGINS_AND_RESOURCES_PATH) + "plugins.cfg");
+	String pluginDir = cfg.getSetting("PluginFolder");
+	struct stat sb;
+	if (stat((pluginDir + "Plugin_CgProgramManager.so").c_str(), &sb) == 0)
+		mRoot->loadPlugin(pluginDir + "Plugin_CgProgramManager");
+
 	// Random seed
 	srand(time(NULL));
 
